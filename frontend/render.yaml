services:
  - type: web
    name: skillstarter-frontend
    env: static
    staticPublishPath: ./build  # Update this path to match your build output directory
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
